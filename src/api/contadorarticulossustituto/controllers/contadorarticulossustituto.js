'use strict';

/**
 *  contadorarticulossustituto controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::contadorarticulossustituto.contadorarticulossustituto');
