'use strict';

/**
 * contadorarticulossustituto router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::contadorarticulossustituto.contadorarticulossustituto');
