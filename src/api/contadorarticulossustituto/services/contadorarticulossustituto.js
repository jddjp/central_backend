'use strict';

/**
 * contadorarticulossustituto service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contadorarticulossustituto.contadorarticulossustituto');
